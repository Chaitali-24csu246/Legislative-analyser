# ── Core app ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── LLM / Agent framework ─────────────────────────────────────────────────────
langchain-ollama>=0.1.3
langgraph>=0.1.19

# ── PDF extraction ────────────────────────────────────────────────────────────
pymupdf4llm>=0.0.17          # Markdown-quality PDF → text (wraps PyMuPDF)
pymupdf>=1.24.0              # PyMuPDF backend (pulled in by pymupdf4llm)

# ── Env / config ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Standard-library back-fills (only needed on Python < 3.11) ────────────────
# typing_extensions>=4.9.0   # uncomment if you see TypedDict errors on 3.9/3.10
